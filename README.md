# Hybrid IDS Framework

**A Hybrid Deep Learning Anomaly-Based Intrusion Detection System**  
Autoencoder + SSA-optimized LSTM + LOF + SMOTE + SHAP explainability

## Quick Start
```bash
git clone https://github.com/0xc1GenZ/hybrid-ids-framework.git
cd hybrid-ids-framework
python -m venv env
source env/bin/activate    # Windows: env\Scripts\activate
pip install -r requirements.txt
